# Do not place real passwords, private keys or tokens in this repository.
# Complete these names only after selecting the stack and service configuration.

# APP_ENV=development
# GATEWAY_PORT=8080
# IDENTITY_SERVICE_PORT=8081
# RESOURCE_SERVICE_PORT=8082
# TOKEN_ISSUER=
# TOKEN_AUDIENCE=
# TOKEN_SIGNING_KEY=replace-with-a-local-secret
